# AirShatter Dependency List
# Install with your package manager (apt, pacman, etc.)

aircrack-ng      # core suite (airodump, aireplay, etc.)
hashcat          # password auditing
hcxtools         # hcxpcapngtool (handshake extraction)
iw               # interface management
iproute2         # ip link (interface control)
xterm            # terminal multiplexing / separate windows
ethtool          # driver detection
wireshark        # (optional) capture analysis
tshark           # (optional) terminal-based analysis
tcpdump          # (optional) packet capture
